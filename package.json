{
  "name": "musicsync",
  "version": "1.0.0",
  "private": true,
  "description": "MusicSync - A lightweight, open-source music library synchronizer controlled through Telegram",
  "scripts": {
    "start": "node server.ts",
    "dev": "node server.ts",
    "test": "cd backend && npm test",
    "build": "cd backend && npx tsc --noEmit",
    "lint": "cd backend && npx tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240923.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "wrangler": "^3.0.0"
  }
}
